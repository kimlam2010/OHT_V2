# Source code CMakeLists.txt
# ISO/IEC 12207 - Implementation Layer

# Add subdirectories
add_subdirectory(hal)
add_subdirectory(app)
add_subdirectory(utils)

# Minimal API demo binary removed - using main firmware only
